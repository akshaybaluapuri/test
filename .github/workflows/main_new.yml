# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY_TEST }} 
          known_hosts: "TestingCD"
          
      - name: Adding Known Hosts
        run: sudo ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
        
      - name: checkout action
        uses: actions/checkout@v2

      - name: "Build and push celery image"
        uses: azure/docker-login@v1
        with:
          login-server: equifibetabackend.azurecr.io
          username: equifibetabackend 
          password: v+JFRF1gegvZ46N9jOTbUXqvVHDiVwso
      - run: |
          docker tag httpd equifibetabackend.azurecr.io/httpd
          docker push equifibetabackend.azurecr.io/httpd
          
     
      - name: executing remote ssh commands using ssh key
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY_TEST }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd /home/test/testfolder/test
            docker stop myapp_new
            docker rm myapp_new
            docker build -t httpd_new_test .
            docker run -dit --name myapp_new -p 5000:80 httpd_new_test
