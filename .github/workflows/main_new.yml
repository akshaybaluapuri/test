# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY_TEST }} 
          known_hosts: "TestingCD"
          
      - name: Adding Known Hosts
        run: sudo ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
        
      - name: checkout action
        uses: actions/checkout@v2

      - name: "Build and push celery image"
        uses: azure/docker-login@v1
        with:
          login-server: equifibetabackend.azurecr.io
          username: equifibetabackend 
          password: v+JFRF1gegvZ46N9jOTbUXqvVHDiVwso
      - run: |
            docker build . -t equifibetabackend.azurecr.io/demo:${{ github.sha }}
            docker push equifibetabackend.azurecr.io/demo:${{ github.sha }}
                  
      - name: "Deploy Celery to Azure Container Instances"
        uses: "azure/aci-deploy@v1"
        with:
          resource-group: equifi
          # dns-name-label: ${{ secrets.RESOURCE_GROUP }}celery
          image: equifibetabackend.azurecr.io/demo:41b8c5fb6d9abe0cb7da1ef278299d022d2e6091
          registry-login-server: equifibetabackend.azurecr.io
          registry-username: equifibetabackend 
          registry-password: v+JFRF1gegvZ46N9jOTbUXqvVHDiVwso
            
